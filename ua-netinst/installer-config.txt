preset=base
packages=
mirror=http://mirrordirector.raspbian.org/raspbian/
release=jessie
hostname=FirePick
boot_volume_label=FireHawk
domainname=
rootpw=
disable_root=1
username=fireuser
userpw=firehawk
user_is_admin=1
cdebootstrap_cmdline=
bootsize=+96M # /boot partition size in megabytes, provide it in the form '+<number>M' (without quotes)
rootsize=     # / partition size in megabytes, provide it in the form '+<number>M' (without quotes), leave empty to use all free space
timeserver=time.nist.gov
timezone=ETC/Utc # set to desired timezone (e.g. America/Edmonton)
locales=en_CA.UTF-8  # a space delimited list of locales that will be generated during install (e.g. "en_US.UTF-8 nl_NL sl_SI.UTF-8")
system_default_locale=en_US.UTF-8 # the default system locale to set (using the LANG environment variable)
if_name= # By default this will use whatever interface can be used for a network connection (eth0 for ethernet)
ip_addr=dhcp
ip_netmask=0.0.0.0
ip_broadcast=0.0.0.0
ip_gateway=0.0.0.0
ip_nameservers=0.0.0.0 # if you would like to use a specific DNS, enter it here
drivers_to_load= # load specific kernel modules here
online_config= # URL to extra config that will be executed after installer-config.txt
usbroot= # set to 1 to install to first USB disk
cmdline="dwc_otg.lpm_enable=0 console=ttyAMA0,115200 kgdboc=ttyAMA0,115200 console=tty1 elevator=deadline"
rootfstype=ext4
